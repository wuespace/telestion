import { Given, When, Then } from '@cucumber/cucumber';

When(/^I log in with invalid credentials$/, function () {});
Given(/^I am logged out$/, function () {
	return 'pending';
});
Then(/^I see the login page$/, function () {});
Given(/^I am logged in$/, function () {});
When(/^I log out$/, function () {});
